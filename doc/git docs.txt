On /var/www/html/doliclone :

I got errors on last in last ftp push in file : "errors last git ftp.txt"

$ git remote -v
bafbes	https://bafbes@github.com/bafbes/dolibarr.git (fetch) (push)
origin	https://github.com/Dolibarr/dolibarr.git (fetch)(push)
usb	/media/abb/USB/dolibarr.git (fetch)(push)
local	/media/abb/mes donnees/gitclones/dolibarr.git (fetch)t push)

to get files before 4.0
$ git diff --name-only 4.0

to get files before 4.0 and compress them
$ git diff --name-only 4.0|tar -zcvf -T archive-name.tar.gz
